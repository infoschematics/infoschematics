/**
 * The corner every component-scale shape takes: a card, a fabric's frame, the
 * shell of one drawn as a rectangle, and the clasp of an adapter. Stating it
 * once is what stops a fabric curving unlike the cards beside it, which read as
 * two kinds of thing rather than one stage.
 */
export const cornerRadius = 10
