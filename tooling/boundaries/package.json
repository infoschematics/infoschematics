{
  "name": "infoschematics-boundary-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Install root for the dependency-boundary checker, which needs a TypeScript dependency-cruiser can drive. Not a workspace member, and nothing else resolves it.",
  "dependencies": {
    "dependency-cruiser": "^18.3.1",
    "typescript": "^6.0.3"
  }
}
