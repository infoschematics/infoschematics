---
id: INFOSCHEMATICS-TOOL-020
area: TOOL
title: JSON and YAML definitions
theme: tool
horizon: next
status: done
blocks: [INFOSCHEMATICS-TOOL-021]
blocked_by: []
baseline_ref: 05f7c569592dbdd346aaa596f6573115e996411a
---

## Goal

Let an Infoschematic be authored as TypeScript, JSON, or YAML and validated identically in all three, so authoring an Infoschematic does not require writing or compiling a TypeScript module.

## Context

`InfoschematicConfig` is already pure serialisable data. Round-tripping `examples/is-infoschematics` through `JSON.parse(JSON.stringify(config))` and re-rendering it with `@infoschematics/render-svg` produces byte-identical SVG, the structure contains no `null` holes, and `defineInfoschematic(JSON.parse(text))` normalises a parsed object exactly as it normalises a literal. The domain contract is therefore already format-neutral; nothing in `packages/domain-model` or `packages/domain-core` requires a definition to arrive as TypeScript.

What is missing is the boundary that turns an untrusted document into an `InfoschematicConfig`. `defineInfoschematic` normalises a value it already trusts to have the right shape: TypeScript supplies that guarantee at compile time, and a parsed JSON or YAML document does not. Today an authored example is a workspace package exporting a typed literal, so the type checker is the only validator in the repository, and `scripts/render-example.ts` can only render from a fixed registry of imported modules.

## Boundary

TOML is explicitly out of scope; the supported set is TypeScript, JSON, and YAML.

This item does not publish the renderer as an installable command — that is [INFOSCHEMATICS-TOOL-021](INFOSCHEMATICS-TOOL-021-publish-renderer-command.md), which depends on the loader landing first. It does not change `InfoschematicConfig`, add runtime behaviour to authored definitions, or migrate the existing examples away from TypeScript; TypeScript authoring stays first-class and keeps its compile-time guarantee.

## Current state

The domain contract already round-trips through JSON without changing rendered output, but the repository has no supported boundary for parsing, validating, and diagnosing an untrusted authored document.

## Steps

- [x] Add the Zod schema for `InfoschematicConfigInput` to Domain Core, with a bidirectional type-parity assertion against the hand-written contract.
- [x] Implement `parseInfoschematic` over JSON and YAML with path-specific diagnostics.
- [x] Extend the example renderer loader to accept a pathname in any supported format.
- [x] Emit the JSON Schema through a repository-owned script and commit its output.
- [x] Add format-parity and malformed-document tests, and record the schema-source decision.

## Files touched

- `packages/domain-core/src/schema.ts` and `packages/domain-core/src/parse.ts`, new, with their tests
- `packages/domain-core/package.json` for the `zod` and `yaml` dependencies and the schema export
- `scripts/render-example.ts` for file-based loading, and a new schema-emitting script
- `packages/domain-model/` is **not** touched: the schema-source decision keeps type ownership where it is

## Verify

Prove that equivalent TypeScript, JSON, and YAML definitions render byte-identical SVG. Reject malformed documents with a path-specific diagnostic, and run the affected package tests and type-checks.

## Dependencies / blocks

The schema-source decision is the first step and gates the remaining implementation because it determines whether domain types remain hand-written.

## Documentation impact

### Decision Records

Record the schema-source choice if it establishes a durable ownership or architecture boundary.

### Specifications

Specify the supported document formats, validation boundary, and diagnostic guarantees.

### Guides

Document file authoring and editor schema integration alongside the published entry point.

### Roadmap

Keep INFOSCHEMATICS-TOOL-021 blocked until the validated loader contract lands.

## Review

### Delivered

An Infoschematic can now be authored as TypeScript, JSON, or YAML, and the three are validated identically. `parseInfoschematic` in Domain Core turns an untrusted document into a normalised `InfoschematicConfig` or a list of path-addressed diagnostics; `infoschematicConfigSchema` is a Zod mirror of `InfoschematicConfigInput` held to it by a bidirectional compile-time parity assertion; the same schema projects the JSON Schema an editor consumes, which is generated by a repository-owned script and committed. `self:examples:render` accepts a document pathname alongside its registered example names.

TOML remains out of scope, `InfoschematicConfig` is unchanged, Domain Model is untouched and still dependency-free, and the existing examples remain TypeScript.

### Summary of changes

- `packages/domain-core/src/schema.ts` — new. `infoschematicConfigSchema`, a strict Zod mirror of the whole contract, `infoschematicJsonSchema()` projecting it through `z.toJSONSchema()`, and `SchemaMirrorsContract`, the parity assertion. `PortId` is restated with `z.templateLiteral` rather than a hand-written pattern, so the inferred type is the contract's own and the emitted JSON Schema carries the same rule.
- `packages/domain-core/src/parse.ts` — new. `parseInfoschematic(text, { format?, pathname? })` returning a discriminated result, `infoschematicFormatOf`, `infoschematicFormatExtensions`, and `formatInfoschematicIssue`. Syntax errors, contract violations, and `defineInfoschematic`'s own referential checks all arrive as issues in one shape. A `$schema` key is removed as editor metadata before validation.
- `packages/domain-core/src/index.ts` — now a thin root re-export; `defineInfoschematic` moved unchanged to `define.ts`. This split is not in the plan's file list and was forced by it: `parse.ts` needs `defineInfoschematic`, and the plan requires the schema and loader be reachable from the package root, so leaving the normaliser in `index.ts` would have created an import cycle that `no-circular` rejects.
- `packages/domain-core/package.json` — `zod@^4.5.4` and `yaml@^2.9.0` as dependencies. **No new export entry**: see the first outstanding concern.
- `packages/domain-core/schema/infoschematic.schema.json` — new, generated and committed.
- `scripts/generate-schema.ts` — new, with `--check` and `--out`, following the shared `scripts/cli.ts` contract. Wired as `self:schema:generate` and `self:verify:schema`, and the verify step is now part of `self:check`, so the committed file cannot go stale.
- `scripts/render-example.ts` — `loadRenderable` resolves a subject as a registered example or as a document pathname; the operand is renamed `subject`; `--all` with operands is now rejected as misuse rather than silently ignoring them.
- `scripts/fixtures/format-parity.{ts,json,yaml}` and `malformed.yaml` — new. The same definition authored three times by hand, so the parity test compares three independent documents rather than a serialiser against itself.
- `biome.json` — `packages/domain-core/schema` excluded. The file's bytes belong to the generator; the formatter had rewritten it and made `self:verify:schema` fail, and `lint-staged` would have done so again on every commit.
- Documentation — [ADR-INFOSCHEMATICS-013](../decisions/ADR-INFOSCHEMATICS-013-validation-mirrors-the-contract.md), a new [Domain Core specification](../specs/domain-core.md) (CORE-001 to CORE-006), an authoring-guide section, and README updates.

### Verification

- `bun run self:check` exit 0: 56 test files, 370 tests, both generated artefacts current, every TypeScript workspace clean, dependency-cruiser clean at 205 modules and 481 dependencies, production site build in 8.47s.
- `bun run self:release:verify` — clean-consumer smoke passed for all 7 packages, with the new external dependencies resolving in a packed consumer.
- `bunx biome check` — 0 errors; the 60 remaining warnings are pre-existing and none are in a file this change touched. `bunx syncpack format --check` clean. `bunx knip` reports only its two pre-existing configuration hints. `bunx rumdl check` clean over every Markdown file changed.
- `ki repo audit --skill ki-engineering --repo .` — PASS.
- Format parity is proved by rendering, not by comparing objects: `scripts/format-parity.test.ts` renders the TypeScript, JSON, and YAML forms through `@infoschematics/render-svg` and asserts byte-identical SVG.
- The parity assertion was proved to bite before being relied on. Mutual assignability was tried first and rejected: deleting an optional field from the schema still type-checked. Exact type identity over structurally flattened types catches all three drifts — a dropped optional field, an invented field, and a forgotten `.readonly()` — each of which was introduced and observed to fail the type-check, then reverted.
- Malformed documents were exercised in each distinguishable way — unparseable JSON, unparseable YAML, a wrong scalar type at a known path, a missing required field, an unrecognised key, and a Card naming an undeclared Domain — and the CLI was run end to end: exit 0 for a valid document, 1 for an invalid one with the path-addressed diagnostic printed, 2 for an unsupported extension.

### Outstanding concerns

- **The JSON Schema is a repository file, not a published package export.** Exporting it was attempted and `self:packages:pack-smoke` rejected it: the release contract in ADR-INFOSCHEMATICS-010 requires every export to expose ESM runtime JavaScript and declarations, and admits only `dist/` into a tarball. Rather than weaken that contract in passing, the export was withdrawn and the guide points at the repository path. A consumer outside this repository has no supported way to reference the schema. Admitting non-JavaScript exports to the release contract is a decision in its own right and is not made here.
- **Strict objects are a compatibility constraint.** Rejecting an unknown key is right for a hand-edited document, but it means a document authored against a newer contract is rejected rather than partially understood. This is recorded in the ADR and as a gap in the specification; no document-versioning scheme is specified.
- **Validation covers shape, not the model's referential rules.** Beyond the Domain checks `defineInfoschematic` already performed, the gaps listed in the Domain Model specification — identifier uniqueness, relationship targets, port availability, placement within geography — remain unvalidated for documents exactly as they do for TypeScript. A document can be schema-valid and still name a Flow endpoint that does not exist.
- **The decision was recorded as an ADR, not a GDR.** The plan said Governed Decision Record, but the reason it gave — that the decision fixes where validation lives and why the contract package stays dependency-free — is architectural, and this repository's taxonomy puts that in an ADR. Reversible by rename if the reviewer disagrees.

### Post-change review

The plan's premise held: the contract really was already format-neutral, and validation really was the whole cost. Two things it did not anticipate shaped the result.

The first is the parity assertion. The plan asked for one and said what it should catch; it did not say how strong the comparison had to be, and the obvious implementation is too weak to do the job. Mutual assignability accepts a schema that has quietly dropped an optional field, which is the most likely drift and the least visible. The assertion is only worth its duplication cost because the comparison is exact, and that is now written down in both the ADR and CORE-003.

The second is that the release contract and the editor-schema goal genuinely conflict. The plan assumed committing the schema was enough for an editor to consume it; that is true inside this repository and false outside it. The conflict is real rather than incidental, and it belongs to whoever decides what a published Infoschematics package may contain.

The `index.ts` split is a small structural change the plan did not name. It is the minimum that satisfies both the root-export decision and `no-circular`, and it leaves `defineInfoschematic` byte-identical.

### Mini recap

INFOSCHEMATICS-TOOL-020 delivers the JSON and YAML authoring boundary. Domain Core gained a strict Zod mirror of the contract under an exact-identity parity assertion, a `parseInfoschematic` loader returning path-addressed diagnostics rather than throwing, and a JSON Schema projected from that same schema and committed. The renderer script loads documents by pathname, and `self:check` now fails if the committed schema goes stale.

Verification is `bun run self:check` at exit 0 with 370 tests, `self:release:verify` clean across 7 packages, a passing `ki repo audit`, and byte-identical SVG rendered from all three formats. The parity assertion was deliberately broken three ways and observed to fail before being trusted.

Four concerns are open for review: the schema cannot be published under the current release contract, strict validation constrains future document versioning, referential validation remains a gap shared with TypeScript authoring, and the decision was filed as ADR-INFOSCHEMATICS-013 rather than as the GDR the plan named. INFOSCHEMATICS-TOOL-021 is unblocked by the loader landing.

## Done

Accepted 2026-09-08 by Kris Brown on the review packet above.

## Discussion

### Schema-source decision

**Zod is adopted schema-first inside Domain Core; Domain Model keeps owning the types and stays dependency-free.**

The item left this open between generating a JSON Schema from `InfoschematicConfig` and adopting a schema-first library whose inferred type replaces the hand-written one. The second reading is rejected on an architectural ground the repository already holds: `@infoschematics/domain-model` is the dependency-free contract, and a schema-first library whose inferred type _replaced_ `InfoschematicConfig` would put a runtime dependency inside that contract and move type ownership out of the package the architecture guide names as its owner. The first reading — generating a schema from the types — needs a second toolchain (a TypeScript-to-JSON-Schema generator plus a JSON Schema validator) to produce diagnostics that Zod gives directly.

What is adopted is neither exactly: the Zod schema lives in Domain Core beside `defineInfoschematic`, mirrors the hand-written types rather than replacing them, and is held to them by a **bidirectional compile-time parity assertion** so the mirror cannot drift silently — a field added to `InfoschematicConfigInput` and not to the schema fails the type-check, and so does the reverse. `zod@4` also emits JSON Schema directly through `z.toJSONSchema()`, so the published editor schema comes from the same single source without a second generator.

`zod` and `yaml` both already resolve in the workspace, and both become explicit Domain Core dependencies. `yaml` (eemeli) is chosen over `js-yaml` because its default `parse` constructs only plain data — it has no schema that instantiates arbitrary types, so there is no unsafe-load footgun to remember at an untrusted boundary.

The parse boundary is one exported function rather than three: `parseInfoschematic(text, { format })`, where an omitted format is inferred from a supplied pathname. It returns a discriminated result rather than throwing, because the caller at a file boundary usually wants to print a diagnostic rather than catch an exception.

### Implementation notes

The intended pass will:

- Add `packages/domain-core/src/schema.ts` holding `infoschematicConfigSchema`, a Zod mirror of `InfoschematicConfigInput`, and the parity assertion that binds the two. Export it from the package root so a host can validate without reaching into a subpath.
- Add `packages/domain-core/src/parse.ts` holding `parseInfoschematic`, which selects the parser by format or pathname extension, parses, validates, and on success passes the value through `defineInfoschematic` so a parsed document and a TypeScript literal arrive at the same normalised config.
- Report diagnostics as `{ ok: false, issues }`, each issue carrying the dotted path, the message, and the offending document. A YAML syntax error is reported in the same shape as a schema violation, so a caller has one thing to print.
- Extend `scripts/render-example.ts` so its operand accepts a pathname as well as a registered example name, resolving by extension and keeping every named example working unchanged. The script keeps the shared CLI contract from `scripts/cli.ts`.
- Add a repository-owned `self:schema:generate` script that writes the emitted JSON Schema, and commit its output so an editor can consume it from the repository without a build.
- Cover format parity by rendering the same definition authored three ways through `@infoschematics/render-svg` and asserting byte-identical SVG, and cover rejection with documents that are malformed in each of the distinguishable ways: unparseable syntax, a wrong scalar type at a known path, and a missing required field.
- Record the schema-source decision as a Governed Decision Record, since it fixes where validation lives and why the contract package stays dependency-free.

### Why validation cannot simply be a cast

`defineInfoschematic` is total over well-typed input, so it will happily normalise a structurally wrong object into a config that renders as an empty or subtly incorrect diagram. That is acceptable when TypeScript has already proved the shape and unacceptable at a file boundary, where the common case is a typo in a hand-edited document. Validation is the whole cost of the feature; parsing is trivial.

### Why TOML is excluded

TOML's table syntax expresses deeply nested heterogeneous arrays — which is exactly what a `regions`, `cards`, and `flows` document is — far more awkwardly than YAML, so it would carry a dependency and a supported-format promise for a worse authoring experience than the two formats already chosen.
